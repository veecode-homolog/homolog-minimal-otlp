apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: homolog-minimal-otlp-location
  description: Location to import all homolog-minimal-otlp catalog components
spec:
  targets:
    - ./.platform/*.yaml
